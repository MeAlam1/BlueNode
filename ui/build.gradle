plugins {
    id 'org.openjfx.javafxplugin' version "${javafx_plugins_version}"
}

javafx {
    version = javafx_version
    modules = javafx_modules.split(',').collect { it.trim() }
}

dependencies {
    implementation project(':core')
    implementation project(':nodes')

    implementation "org.openjfx:javafx-controls:${javafx_dependency_version}"
    implementation "org.openjfx:javafx-fxml:${javafx_dependency_version}"
    implementation "org.openjfx:javafx-media:${javafx_dependency_version}"
    implementation "org.openjfx:javafx-web:${javafx_dependency_version}"
    implementation "org.openjfx:javafx-swing:${javafx_dependency_version}"
    implementation "org.controlsfx:controlsfx:${controlsfx_version}"
}
